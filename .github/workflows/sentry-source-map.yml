# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
"on":
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install Dependencies
        run: yarn
      - name: Build Project
        env: # Or as an environment variable
          REACT_APP_API_URL: ${{ secrets.REACT_APP_API_URL }}
          REACT_APP_MASTER_KEY: ${{ secrets.REACT_APP_MASTER_KEY }}
          REACT_APP_SENTRY_DNS: ${{ secrets.REACT_APP_SENTRY_DNS }}
        run: yarn build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_SHOP_SCRAPER }}"
          channelId: live
          projectId: shop-scraper
      - uses: actions/checkout@v2
      - name: Create Sentry release
        uses: getsentry/action-release@v1
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          SENTRY_ORG: ${{ secrets.SENTRY_ORG }}
          SENTRY_PROJECT: ${{ secrets.SENTRY_PROJECT }}
        with:
          environment: production
          sourcemaps: "./build/static/js"
          # version: ${{ env.REACT_APP_SENTRY_RELEASE }}
